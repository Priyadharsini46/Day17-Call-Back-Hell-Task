setTimeout(() => {
    const letter = document.querySelector(".count");
    letter.innerText = 10;
    setTimeout(() => {
      const letter = document.querySelector(".count");
      letter.innerText = 9;
      setTimeout(() => {
        const letter = document.querySelector(".count");
        letter.innerText = 8;
        setTimeout(() => {
          const letter = document.querySelector(".count");
          letter.innerText = 7;
          setTimeout(() => {
            const letter = document.querySelector(".count");
            letter.innerText = 6;
            setTimeout(() => {
              const letter = document.querySelector(".count");
              letter.innerText = 5;
              setTimeout(() => {
                const letter = document.querySelector(".count");
                letter.innerText = 4;
                setTimeout(() => {
                  const letter = document.querySelector(".count");
                  letter.innerText = 3;
                  setTimeout(() => {
                    const letter = document.querySelector(".count");
                    letter.innerText = 2;
                    setTimeout(() => {
                      const letter = document.querySelector(".count");
                      letter.innerText = 1;
                      setTimeout(() => {
                        const letter = document.querySelector(".count");
                        letter.innerText = "Happy Independence Day";
                      }, 1000);
                    }, 1000);
                  }, 1000);
                }, 1000);
              }, 1000);
            }, 1000);
          }, 1000);
        }, 1000);
      }, 1000);
    }, 1000);
  }, 1000);
  